(function( $ ) {

    $(function() {

        // Add Color Picker to all inputs that have 'color-field' class

        $( '#bh_bg_color, #bh_icon_color , #bh_border_color, #bh_icon_color, #bh_hbg_color, #bh_hborder_color, #bh_hicon_color' ).wpColorPicker();

    });

})( jQuery );
